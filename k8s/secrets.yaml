---
# NOTE: In production, use sealed-secrets or external secrets manager
# This file is a template - DO NOT commit with real values
apiVersion: v1
kind: Secret
metadata:
  name: mobility-secrets
  namespace: mobility-platform
type: Opaque
stringData:
  # Database
  DATABASE_URL: "postgresql://mobility:CHANGE_ME@postgres:5432/mobility_db"
  
  # Redis
  REDIS_PASSWORD: ""
  
  # JWT
  JWT_SECRET: "CHANGE_ME_TO_A_SECURE_SECRET"
  
  # Stripe
  STRIPE_SECRET_KEY: "sk_test_CHANGE_ME"
  STRIPE_WEBHOOK_SECRET: "whsec_CHANGE_ME"
  
  # Google Maps
  GOOGLE_MAPS_API_KEY: "CHANGE_ME"
  
  # Vero API (Finnish Tax Administration)
  VERO_API_ENDPOINT: "https://api.vero.fi/v1"
  VERO_API_KEY: "CHANGE_ME"
